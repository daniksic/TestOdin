@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<div class="container">
    <div class="row">
        <div class="col-sm-4">
            @{Html.RenderAction("TaskPartial", "Task");}
        </div>
        <div class="col-sm-4">
            @{Html.RenderAction("NotificationPartial", "Notification");}
        </div>
    </div>
</div>